{
  "name": "blippr",
  "version": "1.0.1",
  "private": true,
  "description": "Blippr - mobile-first real-time chat PWA with stranger matching, friends, calls, and media.",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "lint": "npm run lint --workspace client"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
